global:
  resolve_timeout: 5m

route:
  receiver: 'telegram'
  group_by: ['alertname']

receivers:
  - name: 'telegram'
    telegram_configs:
      - bot_token: '8336229283:AAGe2r0ripqkBS3l3H4-XPxY4uJZQRFpx_I'
        chat_id: -7730412580
        parse_mode: 'HTML'
        message: |
          <b>{{ .CommonAnnotations.summary }}</b>
          {{ range .Alerts }}
          <pre>{{ .Annotations.description }}</pre>
          {{ end }}
