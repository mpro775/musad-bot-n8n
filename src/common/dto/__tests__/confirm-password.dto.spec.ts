import { ConfirmPasswordDto } from '../confirm-password.dto';

describe('Confirm-passwordDto', () => {
  it('should be defined', () => {
    expect(ConfirmPasswordDto).toBeDefined();
  });

  // TODO: Add validation tests based on DTO properties
});
