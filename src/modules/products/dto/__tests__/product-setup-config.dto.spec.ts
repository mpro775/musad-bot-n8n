import { ProductSetupConfigDto } from '../product-setup-config.dto';

describe('Product-setup-configDto', () => {
  it('should be defined', () => {
    expect(ProductSetupConfigDto).toBeDefined();
  });

  // TODO: Add validation tests based on DTO properties
});
