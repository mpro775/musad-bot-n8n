import { CreateProductDto } from '../create-product.dto';

describe('Create-productDto', () => {
  it('should be defined', () => {
    expect(CreateProductDto).toBeDefined();
  });

  // TODO: Add validation tests based on DTO properties
});
