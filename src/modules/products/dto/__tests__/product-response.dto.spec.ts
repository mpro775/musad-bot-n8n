import { ProductResponseDto } from '../product-response.dto';

describe('Product-responseDto', () => {
  it('should be defined', () => {
    expect(ProductResponseDto).toBeDefined();
  });

  // TODO: Add validation tests based on DTO properties
});
