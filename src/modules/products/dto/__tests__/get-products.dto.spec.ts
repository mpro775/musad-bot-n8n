import { GetProductsDto } from '../get-products.dto';

describe('Get-productsDto', () => {
  it('should be defined', () => {
    expect(GetProductsDto).toBeDefined();
  });

  // TODO: Add validation tests based on DTO properties
});
