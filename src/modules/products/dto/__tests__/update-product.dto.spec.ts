import { UpdateProductDto } from '../update-product.dto';

describe('Update-productDto', () => {
  it('should be defined', () => {
    expect(UpdateProductDto).toBeDefined();
  });

  // TODO: Add validation tests based on DTO properties
});
