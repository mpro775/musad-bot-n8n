import { ChangePasswordDto } from '../change-password.dto';

describe('Change-passwordDto', () => {
  it('should be defined', () => {
    expect(ChangePasswordDto).toBeDefined();
  });

  // TODO: Add validation tests based on DTO properties
});
