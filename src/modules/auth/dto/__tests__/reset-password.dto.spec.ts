import { ResetPasswordDto } from '../reset-password.dto';

describe('Reset-passwordDto', () => {
  it('should be defined', () => {
    expect(ResetPasswordDto).toBeDefined();
  });

  // TODO: Add validation tests based on DTO properties
});
