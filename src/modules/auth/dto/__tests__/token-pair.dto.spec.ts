import { TokenPairDto } from '../token-pair.dto';

describe('Token-pairDto', () => {
  it('should be defined', () => {
    expect(TokenPairDto).toBeDefined();
  });

  // TODO: Add validation tests based on DTO properties
});
