import { AccessOnlyDto } from '../access-only.dto';

describe('Access-onlyDto', () => {
  it('should be defined', () => {
    expect(AccessOnlyDto).toBeDefined();
  });

  // TODO: Add validation tests based on DTO properties
});
