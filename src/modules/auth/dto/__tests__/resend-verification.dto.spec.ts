import { ResendVerificationDto } from '../resend-verification.dto';

describe('Resend-verificationDto', () => {
  it('should be defined', () => {
    expect(ResendVerificationDto).toBeDefined();
  });

  // TODO: Add validation tests based on DTO properties
});
