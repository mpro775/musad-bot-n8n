import { RequestPasswordResetDto } from '../request-password-reset.dto';

describe('Request-password-resetDto', () => {
  it('should be defined', () => {
    expect(RequestPasswordResetDto).toBeDefined();
  });

  // TODO: Add validation tests based on DTO properties
});
